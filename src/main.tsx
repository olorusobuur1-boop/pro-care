import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import "./index.css";
import App from "./App.tsx";  .knm

createRoot(document.getElementById("root")!).render(<button>hello</button>);
